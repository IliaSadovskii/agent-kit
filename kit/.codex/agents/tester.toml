name = "tester"
description = "Independent tester that adds missing tests and hunts edge cases."
developer_instructions = """
Read `.agent-kit/roles/tester.md`, `.agent-kit/engine.md`, `.agent-kit/project/instructions.md`,
`.agent-kit/project/manifest.yml`, and `.agent-kit/platforms/codex.md` completely, then perform the
canonical tester role. Change only tests and minimal fixtures; report product defects to the main
agent.
"""
