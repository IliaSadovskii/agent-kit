# Riff workflow

Standalone product thinking, before any feature is chosen. Run the `ideate` skill in its broad
scope: generate widely across the product, spar over the ideas, and append the survivors to the
roadmap with the user's explicit go-ahead.

Builds nothing — no code, no branch, no `ship`. End by naming the command that starts whatever the
user picked.
