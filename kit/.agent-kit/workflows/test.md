# Test workflow

"Cover this with tests" for a target the user names — a file, a module, or a behavior — without
running a whole feature build.

Delegate to the `tester` role; its method lives there. Then run the project's declared test and
lint suite and report the result. If a test exposes a real defect, report the defect rather than
bending the test.
