# The container builds its user with these, so files it writes belong to you.
# The platform fills them from `id -u` / `id -g`; copy this file to .env only if
# your ids are not 1000.
UID=1000
GID=1000
