# The container builds its user with these, so files it writes belong to you.
# `make up` passes your real ids from `id -u` / `id -g`, which beat this file;
# it is here so that `docker compose` on its own has something sensible.
UID=1000
GID=1000
