# Project handbook

The heading the contract binds to was renamed from "Boundaries" to "Limits". Until anchors ship,
a rename reads as a missing section — an honest limitation rather than a silent one.

## Limits

What must never enter this project.

## Verification

`true` is the whole suite here.
