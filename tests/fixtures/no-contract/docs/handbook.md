# Project handbook

A project that has never run the interview: documents, but no `.agent-kit/knowledge/contract.yml`.
`--check` names the template it would start from and exits 2. This file exists so the fixture tree
survives a checkout — git does not carry an empty directory.
