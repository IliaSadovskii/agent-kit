{
  "description": "One guard, on Bash, active only while a run of this kit is at a non-terminal step: it refuses to merge a pull request, to force-push, and to push to the default branch. Outside a run it has no opinion and never fires.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/guard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
