# Local scratch
*.kit-new

# Python bytecode
__pycache__/
*.pyc

# Local environment
.env

# The workshop
.venv/
.pytest_cache/
dist/

# Run state — never repository content. Only the state: what a project
# *declares* about itself belongs in the history beside the code, and the
# bench's cases declare projects of their own.
.agent-kit/*/runs/
