name: validate

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      # Manifest and version agreement, skill and agent frontmatter, dangling plugin-root
      # references, the engine size cap, and shell syntax across the payload.
      #
      # KIT_MUTATE turns on the mutation pass over the step gate. It is off by default because it
      # costs a quarter of an hour and a pre-release check nobody waits for is a check nobody runs;
      # here the wall clock is nobody's afternoon, so it runs on every push.
      - name: Validate the plugin
        run: bash scripts/validate.sh
        env:
          KIT_MUTATE: "1"

      - name: Check that the tag matches VERSION
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          version="$(cat VERSION)"
          if [ "$tag" != "$version" ]; then
            echo "tag v$tag does not match VERSION $version" >&2
            exit 1
          fi
