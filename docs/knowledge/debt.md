# Технический долг

Что уже построено и работает не так. Пишут двое: час с владельцем
(`agent-kit knowledge tell`) — с его слов, и ночь партии — из находок ревью.
Строка закрывается удалением: её убирает работа, которая эту строку назвала,
или владелец своей рукой. Ключ в строке — то, чем на неё ссылаются.

## Работает плохо

- Codex CLI и Gemini CLI читают личный AGENTS.md и GEMINI.md оператора, и флага, сужающего чтение до проекта, у них нет — «читать не инструкция» для этих двух провайдеров не держится · `key: 479gb3`

## Не работает

- Уровень codex и gemini_cli ни разу не измерен: обе декларации написаны по документации инструментов, а не на машине, где они стоят — закрывает `agent-kit provider check <имя>` · `key: mmvcwb`
- Потолок контекста ни у одного провайдера не измерен и читателя у него нет: context_used и context_window только печатаются, а уровень A их не наблюдает вовсе · `key: b7wstb`
- OpenCode не отгружен: его `run` берёт подсказку аргументом, а не со stdin, поэтому декларацией он не заводится и ему нужен адаптер поверх `opencode serve` · `key: 4n4j5v`
