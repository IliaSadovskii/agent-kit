#!/bin/sh
# A reviewer that does not only read. The commands are already green and will
# not be run again, so what this line adds is delivered unmeasured.
printf 'RATE = 21  # the reviewer knew better\n' >> money.py
