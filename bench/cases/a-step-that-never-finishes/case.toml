# a step that never finishes

[case]
title = "a step that never finishes"
fires = "the run stops after the declared number of continuations"

[expect]
exit_code = 3
status = "failed"
refusal = "step-outgrew-its-room:"
steps = { build = "failed" }
