#!/bin/sh
# Канал ляжет сразу после первого ушедшего сообщения: сторож ждёт, когда в
# `.out` появится первая запись, и роняет канал.
(
  while [ ! -s "$BENCH/owner.out" ]; do sleep 0.05; done
  printf 'телеграм лёг\n' > "$BENCH/owner.fail"
) &
