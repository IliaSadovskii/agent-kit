# a red test command

[case]
title = "a red test command"
fires = "the gate closes, the step still records passed"

[expect]
exit_code = 5
status = "stopped"
refusal = "gate-closed:"
steps = { verify = "passed" }
