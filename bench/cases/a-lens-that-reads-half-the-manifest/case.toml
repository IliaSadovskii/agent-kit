# the cheap way to look thorough is to answer for the interesting two thirds

[case]
title = "a lens that leaves one of the manifest's dependencies unanswered"
fires = "the one it left out is named, and the answer is refused rather than counted"

[audit]
lens = "dependencies"

[expect]
exit_code = 3
refusal = "not-accounted-for"
