# three features, each waiting for the next

[case]
title = "the needs graph is a loop, so nothing in it could ever start"
fires = "the declaration is refused by name and the loop is named"
no_disarm = """
the loop in the needs graph is the trap, and it is the declaration itself: a batch
without it is a different case, so there is nothing to take away
"""

[batch]
name = "vat"
features = [
  { slug = "rates", brief = "A table of VAT rates", needs = ["receipt"] },
  { slug = "quote", brief = "A price quoted with VAT on it", needs = ["rates"] },
  { slug = "receipt", brief = "A receipt line naming the VAT", needs = ["quote"] },
]

[expect]
exit_code = 2
