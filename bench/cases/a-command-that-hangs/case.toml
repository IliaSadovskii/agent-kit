# a command that hangs and spawns a child

[case]
title = "a command that hangs and spawns a child"
fires = "the child dies with it"

[expect]
exit_code = 5
status = "stopped"
refusal = "gate-closed:"
steps = { verify = "passed" }
