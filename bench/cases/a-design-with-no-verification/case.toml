# a design with no verification

[case]
title = "a design that says nothing about what will prove it"
fires = "the step is refused and the reason names the field — absent, and then empty"

[expect]
exit_code = 3
status = "failed"
refusal = "output-empty-field: verification"
steps = { design = "failed" }
