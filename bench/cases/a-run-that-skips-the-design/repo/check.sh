#!/bin/sh
# The mark the ordering is measured by. This case's kind is never answered, so
# `verify` refuses — and the whole question is asked before a single declared
# command is paid for, which is only visible if a command leaves something
# behind when it runs.
touch ran
exit 0
