# an excuse the change itself contradicts

[case]
title = "a design excusing a kind on a change that says otherwise"
fires = "record refuses before the owner's knowledge is touched, under its own code"

[expect]
exit_code = 5
status = "stopped"
refusal = "why-the-diff-contradicts: types"
steps = { review = "passed", record = "pending" }
